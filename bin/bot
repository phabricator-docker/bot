#!/bin/sh
cd /app;

git pull --quiet origin master;

git submodule --quiet foreach git pull --quiet origin master;

if ! (git diff --quiet) then
  git commit --all -m "Update Phabricator"
  git push origin master
fi
